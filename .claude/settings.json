{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; [[ \"$f\" == *.svelte || \"$f\" == *.ts || \"$f\" == *.js || \"$f\" == *.json || \"$f\" == *.css || \"$f\" == *.html || \"$f\" == *.md ]] && npx prettier --write \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting..."
          }
        ]
      }
    ]
  }
}
