name: Dependabot PR Testing (DISABLED)

# 🚫 WORKFLOW REPLACED
# 
# This workflow has been replaced by the unified pr-validation.yml
# which handles Dependabot PRs along with normal PRs efficiently:
#
# ✅ UNIFIED APPROACH BENEFITS:
# - Single workflow handles both PR types
# - Same test coverage for Dependabot PRs
# - Context-aware validation (no commitlint for Dependabot)
# - Consistent labeling and processing
# - Eliminates redundant workflow logic
#
# 🔄 MIGRATION IMPACT:
# - Dependabot PRs now validated by unified pr-validation.yml
# - Same tests: check, lint, type-check, unit, e2e, build
# - Same labeling: dependency type, update type
# - Same quality assurance standards
#
# This file can be safely removed after unified workflow validation.

on:
  # Original triggers disabled to prevent execution
  # pull_request:
  #   types: [opened, synchronize]

  # Placeholder trigger that will never run automatically
  workflow_dispatch:
    inputs:
      dependabot_testing_disabled:
        description: 'Dependabot testing moved to unified pr-validation.yml'
        required: true
        default: 'Use unified PR validation'
        type: choice
        options:
          - 'Use unified PR validation'

jobs:
  dependabot-testing-disabled:
    name: Dependabot Testing Moved to Unified Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Notify about workflow consolidation
        run: |
          echo "🚫 Dependabot PR testing has been consolidated."
          echo ""
          echo "✅ Now handled by: Unified pr-validation.yml"
          echo "🎯 Tests both: Normal PRs AND Dependabot PRs"
          echo "📈 Benefits: Single workflow, consistent validation"
          echo "🔧 Same tests: lint, type-check, unit, e2e, build"
          echo "🏷️ Same labeling: dependency types, update types"
          echo ""
          echo "All Dependabot PRs now use the unified validation pipeline."
          echo "This legacy file can be safely deleted after validation period."
          exit 1