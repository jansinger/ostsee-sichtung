name: Dependabot Combined PR (DISABLED)

# 🚫 WORKFLOW DISABLED
# 
# This workflow has been replaced by dependabot-combined-optimized.yml
# which eliminates redundant testing and improves CI efficiency by:
#
# - Only processing pre-validated Dependabot PRs
# - Skipping redundant test execution (60-80% time savings)  
# - Maintaining same quality assurance standards
#
# This file can be safely removed after the new workflow is validated.

on:
  # Original triggers disabled to prevent execution
  # schedule:
  #   - cron: '0 9 * * *'  # Daily at 9:00 UTC
  # workflow_dispatch:

  # Placeholder trigger that will never run automatically
  workflow_dispatch:
    inputs:
      force_disabled_workflow:
        description: 'This workflow is disabled. Use dependabot-combined-optimized.yml instead.'
        required: true
        default: 'Please use the optimized version'
        type: choice
        options:
          - 'Please use the optimized version'

jobs:
  disabled-notification:
    name: Workflow Disabled - Use Optimized Version
    runs-on: ubuntu-latest
    steps:
      - name: Notify about workflow replacement
        run: |
          echo "🚫 This workflow has been disabled and replaced."
          echo ""
          echo "✅ Use instead: dependabot-combined-optimized.yml"
          echo "📈 Benefits: 60-80% less CI time, same quality assurance"
          echo "🔗 See: WORKFLOW_OPTIMIZATION_PLAN.md for details"
          echo ""
          echo "This workflow can be safely deleted after validation period."
          exit 1